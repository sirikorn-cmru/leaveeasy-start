// ─────────────────────────────────────────────────────────────
// js/index.js — หน้าแรก
// ทำหน้าที่เดียวคือกันไม่ให้คนที่ยังไม่ล็อกอินเข้ามาดู
// และวาดชื่อคนที่ล็อกอินอยู่ลงแถบเมนู
// ─────────────────────────────────────────────────────────────

import { กันหน้า } from "./auth.js";

await กันหน้า();
