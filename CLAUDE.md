# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ภาพรวม

เว็บระบบขอลาออนไลน์ (LeaveEasy) เขียนด้วย **HTML · CSS · JavaScript ธรรมดา ไม่มี framework ไม่มีขั้นตอน build**
เป็นใบงานของหลักสูตร ADT-RAISE Batch 2 Module 2 — ไม่ใช่ระบบใช้งานจริง

## คำสั่ง

```bash
npm install      # ครั้งเดียวตอนเริ่ม (ลง serve)
npm run dev      # เปิด http://localhost:3000
```

**ไม่มี test และไม่มี linter ในโปรเจกต์นี้** — spec ห้ามเขียนทดสอบอัตโนมัติจนกว่าจะถึงสัปดาห์ที่ 9
วิธีตรวจงานคือเปิดเบราว์เซอร์ดูจริง

⚠️ **ต้องเปิดผ่าน `npm run dev` เท่านั้น** — หน้าที่ใช้ Firestore เป็น ES module เปิดด้วย `file://` (ดับเบิลคลิก) ไม่ได้

## เอกสารที่ต้องอ่านก่อนแก้อะไร

**`leaveeasy-spec.md` คือแหล่งความจริง** — กำหนดชื่อโฟลเดอร์ ชื่อช่องข้อมูล ค่าสถานะ และข้อมูลตัวอย่าง
ก่อนตั้งชื่อช่องใหม่หรือเพิ่มโครงสร้างข้อมูล ให้เปิดหัวข้อ 5 และ 7 ก่อนเสมอ

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| `leaveeasy-spec.md` | ทุกครั้งที่จะเพิ่มโครงสร้างข้อมูลหรือฟีเจอร์ |
| `ACL.md` | ตารางสิทธิ์ 3 บทบาท · พิมพ์เขียวของ Security Rules สัปดาห์ที่ 8 |
| `firestore.rules` | กฎที่ Publish อยู่จริง |

### 🛑 ขอบเขตรายสัปดาห์ — ห้ามทำงานล่วงหน้า

spec หัวข้อ 8 แบ่งงานเป็นสัปดาห์ และหัวข้อ 9 เขียนเตือน AI ไว้ตรง ๆ ว่า **ห้ามสร้างสิ่งที่อยู่นอกขอบเขตขึ้นมาเอง แม้จะเห็นว่าระบบควรมี** ถ้าคิดว่าจำเป็น ให้เสนอแล้วรอคำสั่ง

| สัปดาห์ | ขอบเขต |
|:-:|---|
| 6 | อ่านจาก Firestore อย่างเดียว (R ตัวเดียว) |
| 7 | CRUD ครบ · ล็อกอิน · Firebase Hosting |
| 8 | Security Rules · ปุ่มผู้ช่วย AI |
| 9 | ทดสอบอัตโนมัติ |

**สถานะปัจจุบัน: สัปดาห์ที่ 7 ครบ · สัปดาห์ที่ 8 ทำ Security Rules + ซ่อนปุ่มตามบทบาทแล้ว**

| งานสัปดาห์ที่ 7 | สถานะ |
|---|:-:|
| C สร้างใบลาใหม่ลง Firestore | ✅ |
| R อ่านรายการและรายละเอียด | ✅ |
| U แก้ (อนุมัติ/ไม่อนุมัติ · ส่งความเห็น) | ✅ |
| D ลบ (เฉพาะใบที่ยัง `รอพิจารณา`) | ✅ |
| ล็อกอิน Firebase Authentication | ✅ |
| กฎขั้นต่ำ ต้องล็อกอินก่อนถึงอ่าน/เขียนได้ | ✅ · **สัปดาห์ที่ 8: กฎรายห้องตาม ACL.md ขึ้นใช้งานแล้ว** |
| Firebase Hosting | ✅ https://gen-lang-client-0091628886.web.app |
| `leave-types.html` ต่อ Firestore | ✅ |

⚠️ **ตอนเปลี่ยนสถานะ ต้องใช้ `updateDoc` ส่งไปเฉพาะช่อง `status`** — [spec หัวข้อ 6](leaveeasy-spec.md) สั่งห้ามเขียนทับช่องอื่น
แปลว่ากดอนุมัติแล้ว **ห้ามเติม `approverId`/`approverName`** แม้จะดูสมเหตุสมผลก็ตาม · ถ้าใช้ `setDoc` จะลบอีก 11 ช่องทิ้งทั้งไฟล์

⚠️ **ลบใบลาต้องลบความเห็นในโฟลเดอร์ย่อย `approvals` ให้หมดก่อน** — Firestore ไม่ลบโฟลเดอร์ย่อยตามให้อัตโนมัติ
ถ้าลบแต่ไฟล์แม่ ความเห็นจะค้างเป็นขยะที่มองไม่เห็นทั้งจากหน้าเว็บและจาก Console

### 🔒 Security Rules — บังคับตาม ACL.md แล้ว

กฎรายโฟลเดอร์อยู่ที่ [`firestore.rules`](firestore.rules) · ส่งขึ้นใช้งานด้วย

```bash
npx.cmd firebase-tools deploy --only firestore:rules
```

| โฟลเดอร์ | ใครทำอะไรได้ |
|---|---|
| `users` | ล็อกอินแล้วอ่านได้ · สร้างได้เฉพาะของตัวเองและ `role` ต้องเป็น `employee` · **ห้ามเลื่อนขั้นตัวเอง** · hr แก้ได้ทุกคน |
| `leaveTypes` | ล็อกอินแล้วอ่านได้ · **เพิ่ม/แก้/ลบเฉพาะ hr** |
| `leaveRequests` | เจ้าของหรือผู้อนุมัติอ่านได้ · เปลี่ยน `status` เฉพาะผู้อนุมัติและแก้ได้ช่องเดียว · ลบเฉพาะใบที่ยัง `รอพิจารณา` โดยเจ้าของหรือ hr |
| `approvals` | เขียนในนามตัวเองเท่านั้น · **แก้ไม่ได้ตลอดกาล** · ลบได้เฉพาะตอนใบแม่ยังลบได้ |

🔑 **Rules กรองข้อมูลให้ไม่ได้ — มันปฏิเสธทั้งคำสั่ง**
ถ้าฝั่งหน้าเว็บขอเกินสิทธิ์ จะได้ `permission-denied` ทั้งก้อน ไม่ใช่ได้มาเฉพาะที่มีสิทธิ์
`js/leave-requests.js` จึงต้องใส่ `where("requesterId","==",uid)` ให้ `employee` เอง — **ถ้าลบบรรทัดนั้นออก หน้ารายการจะพังทั้งหน้า**

⚠️ **กฎ 1 ข้อใน ACL.md เขียนใน Rules ไม่ได้** — *"จะกดไม่อนุมัติ ต้องมีความเห็นอย่างน้อย 1 รายการก่อน"*
Rules นับไฟล์ในโฟลเดอร์ย่อยไม่ได้ · ยังบังคับได้แค่ในโค้ดหน้าเว็บ ซึ่งข้ามได้ด้วย F12

⚠️ **จุดที่ผ่อนจาก ACL.md อย่างตั้งใจ** — ACL บอกห้ามลบความเห็น แต่ถ้าปิดสนิทจะลบใบลาที่มีความเห็นไม่ได้เลย
เพราะ Firestore ไม่ลบโฟลเดอร์ย่อยตามให้ · จึงยอมให้ลบเฉพาะตอนใบแม่ยัง `รอพิจารณา` และเป็นเจ้าของหรือ hr

⚠️ **ชื่อฟังก์ชันใน `firestore.rules` เป็นภาษาอังกฤษ** ต่างจากไฟล์ `.js` — ภาษาของ Rules ไม่รับชื่อภาษาไทย

⚠️ **เปลี่ยน `role` ของใครได้จาก Firebase Console เท่านั้น** — ผู้ใช้ตั้งตัวเองเป็น `hr` ไม่ได้ (กฎกันไว้)
ไฟล์โปรไฟล์ของบัญชีจริงคือไฟล์ที่**ชื่อเป็น uid ยาว ๆ** ไม่ใช่ `u001`–`u003` ซึ่งเป็นข้อมูลตัวอย่างที่ล็อกอินไม่ได้

### ซ่อนปุ่มตามบทบาท — ทำแบบ "ซ่อนก่อนแล้วค่อยเปิด"

ของที่มีแต่ `hr` เห็น ให้ใส่ **`class="hidden"` + `data-hr-only`** ไว้ตั้งแต่ตอนวาด
แล้ว `เปิดของเฉพาะฝ่ายบุคคล()` ใน `js/auth.js` จะเอา `hidden` ออกให้ทีหลังเมื่อรู้บทบาทแล้ว

**ห้ามทำกลับกัน** (วาดครบก่อนแล้วค่อยซ่อน) เพราะเมนูจะแวบให้เห็นแล้วหายไป
และถ้า JS พังกลางทาง ของจะค้างอยู่ให้คนที่ไม่มีสิทธิ์เห็น

| ที่ | ใครเห็น |
|---|---|
| เมนู + ปุ่ม **ประเภทการลา** | `hr` |
| ปุ่ม **อนุมัติ / ไม่อนุมัติ** | `manager` · `hr` |
| ปุ่ม **ลบใบลา** | เจ้าของใบ หรือ `hr` |
| หน้า `leave-types.html` สำหรับคนอื่น | ตารางแบบ**ดูอย่างเดียว** ไม่มีคอลัมน์จัดการ |

⚠️ **ข้อความบนหน้าจอต้องตรงกับสิ่งที่เห็นจริงด้วย** — หน้ารายการเขียนว่า "ใบขอลาของคุณ" ให้ `employee`
และ "ใบขอลาทั้งหมดในระบบ" ให้ `manager`/`hr` · อย่าปล่อยข้อความที่พูดไม่ตรงกับข้อมูลที่กรองแล้ว

⚠️ **ปุ่มที่ `disabled` ไม่ใช่การกันสิทธิ์** — เปิด F12 สั่ง Firestore ตรง ๆ ข้ามได้เสมอ · การกันจริงอยู่ใน Rules เท่านั้น

ข้อห้ามถาวรจาก spec หัวข้อ 9: ไม่ใช้ framework · ไม่เขียนเซิร์ฟเวอร์เอง · ไม่ใช้ SQL · ไม่ทำ pagination · **ห้ามใส่ข้อมูลจริงของบุคคล**

## สถาปัตยกรรม

### สคริปต์ classic กับ module อยู่ร่วมกัน — จุดที่ต้องเข้าใจก่อนแก้ HTML

**หน้าที่แสดงข้อมูลทั้ง 4 หน้า** โหลด `js/util.js` และ `js/nav.js` เป็น **classic script + `defer`** ซึ่งประกาศฟังก์ชันไว้บน `window`
(`esc()` · `ป้ายสถานะ()` · `เวลาตอนนี้()` · `ค่าจากURL()` · `ข้อความผิดพลาดฐานข้อมูล()`)

⚠️ **`index.html` โหลดแค่ `nav.js` + `js/index.js`** เพราะไม่มีข้อมูลให้วาด — ถ้าจะเรียก `esc()` ในหน้านั้น ต้องเติม `util.js` เข้าไปก่อน
⚠️ **`login.html` และ `signup.html` ไม่โหลดทั้ง `nav.js` และ `util.js`** — ตั้งใจ เพราะยังไม่ล็อกอินจึงไม่ควรมีแถบเมนู

**ตอนนี้สคริปต์ประจำหน้าเป็น `type="module"` ทั้งหมดแล้ว** และ **ไม่มีหน้าไหนโหลด `js/data.js` อีกต่อไป**
(`js/data.js` ยังอยู่ในโปรเจกต์เพราะ spec หัวข้อ 0 ระบุไว้ในโครงไฟล์ แต่ไม่มีใครใช้แล้ว)

| หน้า | สคริปต์ | แหล่งข้อมูล |
|---|---|---|
| `leave-requests.html` | `type="module"` | **Firestore** (อ่านอย่างเดียว) |
| `leave-request-detail.html` | `type="module"` | **Firestore** (อ่าน · แก้ `status` · เพิ่มความเห็น · ลบใบลา) |
| `new-leave-request.html` | `type="module"` | **Firestore** (อ่าน `leaveTypes` · เขียนใบลาใหม่) |
| `leave-types.html` | `type="module"` | **Firestore** (อ่าน · เพิ่ม · แก้ชื่อ · ลบ) |
| `index.html` | `type="module"` (`js/index.js`) | ไม่มีข้อมูล · มีแค่ยามเฝ้าหน้า |
| `login.html` · `signup.html` | `type="module"` | Firebase Auth · **ไม่มียามเฝ้าหน้า ไม่มีแถบเมนู** |

**ที่ classic กับ module อยู่ร่วมกันได้เพราะ** classic `defer` ทำงานก่อน module เสมอตามลำดับที่เขียนใน HTML
module จึงเรียกฟังก์ชันจาก `util.js` ได้ · **เวลาสร้างหน้าใหม่ ให้คง `util.js` กับ `nav.js` เป็น `defer` ไว้เหมือนเดิม**

`js/firebase.js` เป็น module ที่ `export const db` — หน้าที่ต้องใช้ Firestore เขียน `import { db } from "./firebase.js"`

### ⚠️ เลข version ของ Firebase SDK เขียนตายตัวอยู่ 8 ที่

CDN ของ Firebase ต้องระบุ version ในตัว URL และตอนนี้เป็น **12.18.0** กระจายอยู่

| ไฟล์ | จำนวน import |
|---|:-:|
| `js/firebase.js` (`app` + `firestore` + `auth`) | 3 |
| `js/auth.js` (`auth` + `firestore`) | 2 |
| `js/leave-requests.js` · `js/leave-request-detail.js` · `js/new-leave-request.js` | 1 ต่อไฟล์ |

**อัปเกรดทีต้องแก้ให้ครบทุกที่** ถ้าแก้ไม่ครบจะโหลด SDK คนละรุ่นมาปนกันในหน้าเดียว ซึ่งพังแบบหาสาเหตุยาก
ตรวจว่าครบด้วย `grep -rn "firebasejs/" js/`

### ล็อกอิน — `js/auth.js` เป็นตัวกลางของทุกหน้า

```
รอผู้ใช้()   รอให้ Firebase ตอบว่าใครล็อกอินอยู่ (ฟังครั้งเดียวแล้วเลิกฟัง)
กันหน้า()    ยามเฝ้าหน้า · คืนโปรไฟล์ { uid, email, name, role } หรือเด้งไป login.html
สมัคร() เข้า() ออก()
```

**ทุกหน้าที่ต้องล็อกอิน ต้อง `await กันหน้า()` ก่อน แล้วค่อยเริ่มทำงาน**

```js
var ผู้ใช้ = await กันหน้า();
if (ผู้ใช้) { เริ่มทำงาน(); }
```

**ห้ามวาดข้อมูลก่อนรอผล** ไม่งั้นข้อมูลจะแวบให้คนที่ยังไม่ล็อกอินเห็นก่อนถูกเด้งออก

⚠️ **`login.html` และ `signup.html` ห้ามเรียก `กันหน้า()`** จะเด้งหาตัวเองไม่รู้จบ
สองหน้านี้จึงไม่โหลด `nav.js` และ `util.js` ด้วย — ตั้งใจให้เป็นแบบนั้น

⚠️ **`role` ตอนสมัครเป็น `employee` เสมอ** ([spec US-08](leaveeasy-spec.md:136)) ผู้สมัครเลือกเองไม่ได้ · ถ้าต้องการ `manager` หรือ `hr` ให้ไปแก้ใน Firebase Console

⚠️ **รหัสผู้ใช้มี 2 แบบปนกันในฐาน — ตั้งใจให้เป็นแบบนี้**
ข้อมูลตัวอย่างใช้ `u001`–`u003` ส่วนบัญชีที่สมัครใหม่ได้ uid ยาว ๆ จาก Firebase ซึ่งกำหนดเองไม่ได้
สัปดาห์นี้ยังไม่กรองตามคนจึงไม่มีผล · **ค่อยตัดสินใจตอนสัปดาห์ที่ 8** ว่าจะลบใบเก่าหรือแก้ `requesterId` ให้ชี้ uid จริง

### โครงสร้างข้อมูลบน Firestore

**มีทั้งหมด 4 โฟลเดอร์ ไม่มีมากกว่านี้** — 3 โฟลเดอร์หลัก + 1 โฟลเดอร์ย่อย
ชื่อเขียนติดกันแบบ camelCase ไม่มีขีดล่าง (`leaveRequests` ไม่ใช่ `leave_requests`)

| โฟลเดอร์ | ระดับ | เก็บอะไร |
|---|---|---|
| `users` | หลัก | ผู้ใช้ 3 คน 3 บทบาท (`employee` · `manager` · `hr`) |
| `leaveTypes` | หลัก | ประเภทการลา |
| `leaveRequests` | หลัก | ใบขอลา |
| `approvals` | **ย่อย ซ้อนในใบลาแต่ละใบ** | ความเห็นการอนุมัติของใบนั้น |

⚠️ `approvals` **ไม่ใช่โฟลเดอร์กลาง** ต้องอ้างผ่านใบลาเสมอ: `leaveRequests/{id}/approvals`
ถ้าเจอชื่ออื่นในคำสั่ง (เช่น `comments` `tickets` `categories`) นั่นคือศัพท์ของโปรเจกต์ FixIt ใน spec หัวข้อ 10 — ให้เทียบกลับมาเป็นชื่อข้างบนก่อนลงมือ

```
users/{u001}          { name, email, role }
leaveTypes/{lt001}    { name }
leaveRequests/{lr001} { title, reason, status,
                        requesterId+requesterName, approverId+approverName,
                        leaveTypeId+leaveTypeName, startDate, endDate, createdAt }
  └ approvals/{ap001} { authorId, authorName, message, createdAt }
```

กติกา 3 ข้อที่พังเงียบ ๆ ถ้าทำผิด

1. **รหัสคือชื่อไฟล์ (document ID) ไม่ใช่ช่องข้อมูล** — ห้ามเขียนช่อง `id` ลงในเอกสาร ตอนอ่านให้ประกอบเอง `Object.assign({ id: doc.id }, doc.data())` ถ้าลืม ปุ่มกดแถวจะเปิดใบลาไม่ได้
2. **จดชื่อซ้ำคู่กับรหัสเสมอ** — Firestore ไม่มี JOIN ยอมรับว่าถ้ามีคนเปลี่ยนชื่อใน `users` ใบลาเก่าจะยังขึ้นชื่อเดิม (spec สั่งให้ยอมรับ ไม่ต้องเขียนระบบไล่อัปเดต)
3. **ตัวพิมพ์เล็ก-ใหญ่ของชื่อช่องต้องตรงทั้งระบบ** — `status` กับ `Status` เป็นคนละช่อง

### สถานะผูกกับ CSS โดยตรง

**`status` มีได้ 3 ค่าเท่านั้น — เป็นข้อความภาษาไทย ไม่ใช่รหัสอังกฤษ**

| ค่า | ป้ายสี | เปลี่ยนต่อไปเป็นอะไรได้ |
|---|---|---|
| `รอพิจารณา` | 🟡 เหลือง | `อนุมัติ` หรือ `ไม่อนุมัติ` |
| `อนุมัติ` | 🟢 เขียว | **ปลายทาง เปลี่ยนต่อไม่ได้** |
| `ไม่อนุมัติ` | 🔴 แดง | **ปลายทาง เปลี่ยนต่อไม่ได้** |

- ใบใหม่ทุกใบเริ่มที่ `รอพิจารณา` เสมอ ระบบตั้งให้เอง ผู้ใช้เลือกไม่ได้
- **ห้ามย้อนกลับ** และห้ามเพิ่มค่าที่ 4
- จะเปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นใน `approvals` อย่างน้อย 1 รายการก่อน
- ลบใบลาได้เฉพาะใบที่ยังเป็น `รอพิจารณา`

`ป้ายสถานะ()` ใน `js/util.js` สร้าง class ชื่อ `badge-` + ค่าสถานะ ซึ่งไปตรงกับ `.badge-รอพิจารณา` ฯลฯ ใน `css/style.css`
**แก้ข้อความสถานะเมื่อไหร่ ป้ายสีหายทันทีโดยไม่มี error** ต้องแก้ CSS ตามด้วย

### Firebase Hosting — คำสั่งและกับดัก

```bash
npx.cmd firebase-tools deploy --only hosting        # ขึ้นเว็บ
npx.cmd firebase-tools deploy --only firestore:rules # ส่งกฎจาก firestore.rules
```

⚠️ **บน Windows ต้องใช้ `npx.cmd` ไม่ใช่ `npx`** — PowerShell ตั้งค่าห้ามรันสคริปต์ `.ps1` ไว้เป็นค่าเริ่มต้น
`npx` จะขึ้น `npx.ps1 cannot be loaded because running scripts is disabled` · `npx.cmd` ไม่โดนบล็อก

⚠️ **อย่าพิมพ์ `firebase deploy` เฉย ๆ** จะ deploy ทั้ง hosting และกฎพร้อมกัน · ใส่ `--only` เสมอ

⚠️ **อย่ารัน `firebase init hosting`** จะทับ `firebase.json` ที่ตั้งค่าไว้ โดยเฉพาะ `cleanUrls: false`
ซึ่งกันบั๊กเดียวกับที่ `serve.json` กัน — ถ้าเปิด `cleanUrls` เว็บจริงจะทิ้ง `?id=` จนหน้ารายละเอียดพัง

⚠️ **ดูเลข `found N files` ทุกครั้งที่ deploy** — โปรเจกต์นี้ควรได้ราว ๆ **20 ไฟล์**
เคยพลาดมาแล้ว: `ignore` เขียนแค่ `**/.*` ซึ่งกันได้เฉพาะไฟล์ที่ขึ้นต้นด้วยจุด **แต่ไม่กันของข้างในโฟลเดอร์จุด**
ทำให้ `.git` ทั้งโฟลเดอร์ (124 ไฟล์) หลุดขึ้นเว็บ เปิดอ่านได้จากอินเทอร์เน็ต · แก้ด้วยการเพิ่ม `**/.*/**`
**ถ้าเลขพุ่งเกิน 20 มาก แปลว่ามีอะไรหลุด อย่าเพิ่งปล่อยผ่าน**

### `serve.json` — ห้ามลบ

ตั้ง `cleanUrls: false` ไว้ เพราะค่าเริ่มต้นของ `serve` จะตัด `.html` ออกแล้ว **ทิ้ง query string ไปด้วย**
ทำให้ `leave-request-detail.html?id=lr001` กลายเป็น `/leave-request-detail` และหน้ารายละเอียดหาใบลาไม่เจอ

⚠️ **ถ้าเจอ 404 ทั้งที่ไฟล์มีอยู่จริง ให้สงสัยแคชของเบราว์เซอร์ก่อนเสมอ**
`serve` เคยส่งคำสั่งย้ายถาวร (301) ไปที่ URL ที่ไม่มี `.html` และเบราว์เซอร์จำไว้ **กด F5 ไม่หาย**
แก้ด้วยการเปิด F12 ค้างไว้ → คลิกขวาที่ปุ่มรีเฟรช → **Empty Cache and Hard Reload**
เรื่องนี้เล่นงานมาแล้ว 2 รอบ · ตรวจด้วย `curl -o /dev/null -w "%{http_code}" http://localhost:3000/หน้าที่สงสัย.html` ว่า server ตอบ 200 จริงไหมก่อนไล่หาสาเหตุอื่น

## 🔐 คีย์และความลับ

**`js/firebase.js` มี `apiKey` ของ Firebase อยู่ และถูก push ขึ้น GitHub — อันนี้ถูกต้องแล้ว ห้ามไป "แก้ให้ปลอดภัย"**
Firebase web config ออกแบบมาให้เปิดเผยในหน้าเว็บ ทำหน้าที่แค่ระบุว่าเป็นโปรเจกต์ไหน ตัวที่กันข้อมูลจริงคือ Security Rules (สัปดาห์ที่ 8)
ถ้าย้ายมันไปไฟล์ที่ไม่ถูก push เว็บจะพังทันทีโดยไม่ได้ปลอดภัยขึ้นเลย

**ส่วนคีย์ประเภทนี้ห้าม commit เด็ดขาด ต้องใส่ `.gitignore` ก่อนเสมอ**

| คีย์ | จะเจอตอนไหน | ถ้าหลุด |
|---|---|---|
| **OpenRouter API key** | สัปดาห์ที่ 8 ปุ่มผู้ช่วย AI | คนอื่นเอาไปยิงจนเงินหมด |
| **Firebase service-account JSON** | ถ้าเผลอใช้ firebase-admin | **ยึดโปรเจกต์ได้ทั้งหมด ข้าม Security Rules** |
| รหัสผ่าน · token ส่วนตัวอื่น ๆ | — | — |

**ก่อน push ทุกครั้งให้ดูรายชื่อไฟล์ที่จะขึ้นไปด้วยตาก่อน** (`git diff --cached --name-only`) ตามที่ [.gitignore](.gitignore) เขียนเตือนไว้
ถ้าคีย์หลุดขึ้น repo แล้ว การลบไฟล์ทีหลังไม่พอ — ต้องไปเพิกถอนคีย์นั้นที่ต้นทาง เพราะประวัติ commit ยังเก็บไว้

## ธรรมเนียมการเขียนโค้ด

- **ตัวแปร ฟังก์ชัน และคอมเมนต์เป็นภาษาไทย** ส่วนชื่อช่องข้อมูลและ API ของ Firebase เป็นอังกฤษ — เขียนโค้ดใหม่ให้เข้ากับแบบนี้
- ใช้ `var` + `function` แบบเดิม ไม่ใช้ arrow function หรือ class (ยกเว้น `import`/`async` ในไฟล์ module)
- ทุกข้อความจากผู้ใช้ต้องผ่าน `esc()` ก่อนเอาไปต่อเป็น HTML
- ข้อความ error ต้องบอกสาเหตุเป็นภาษาคน ไม่ปล่อยจอค้างที่ "กำลังโหลดข้อมูล…"
- ข้อความ commit เป็นภาษาไทย

### แบบแผนของทุกปุ่มที่เขียนลงฐานข้อมูล

ทั้ง 3 ไฟล์ที่เขียนลง Firestore ใช้ลำดับเดียวกันหมด — ฟีเจอร์เขียนข้อมูลใหม่ให้ทำตามนี้ด้วย

```
1. ปิดปุ่ม + เปลี่ยนข้อความเป็น "กำลังบันทึก…"   ← กันกดรัวแล้วได้ข้อมูลซ้ำ
2. await เขียนลง Firestore
3. สำเร็จ → ค่อยอัปเดตหน้าจอ หรือเปลี่ยนหน้า
4. พลาด   → อยู่หน้าเดิม · บอกสาเหตุ · เปิดปุ่มคืน · ไม่ล้างข้อมูลที่ผู้ใช้กรอกไว้
```

**หัวใจคือข้อ 3** — ห้ามอัปเดตหน้าจอก่อนแล้วค่อยถอยเมื่อพลาด เพราะผู้ใช้จะเห็นว่าสำเร็จทั้งที่ในฐานไม่มีอะไรเปลี่ยน
