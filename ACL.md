# ACL.md — ตารางสิทธิ์ผู้ใช้ LeaveEasy

ตารางนี้สรุปว่าใครทำอะไรได้บ้างในระบบ LeaveEasy
เนื้อหาอ้างอิงจาก [`leaveeasy-spec.md`](leaveeasy-spec.md) หัวข้อ 2 · **ถ้าสองไฟล์ขัดกัน ให้ยึด spec เป็นหลัก**

---

## ✅ สถานะการบังคับใช้ — บังคับจริงแล้ว

**ตารางข้างล่างบังคับใช้จริงแล้วตั้งแต่สัปดาห์ที่ 8** ทั้งที่ฐานข้อมูลและที่หน้าจอ

| สัปดาห์ | บังคับใช้แค่ไหน |
|:-:|---|
| 6 | ไม่มีล็อกอิน · ทุกหน้าจอเปิดใช้ได้หมด |
| 7 | ล็อกอินแล้ว · กฎขั้นต่ำ "ต้องล็อกอินก่อน" · ยังไม่แยกตามบทบาท |
| **8 (ตอนนี้)** | **Security Rules รายห้องครบทุกโฟลเดอร์ · ตารางนี้มีผลจริง** |

**กันสองชั้น**

| ชั้น | ทำอะไร | ข้ามได้ไหม |
|---|---|---|
| `firestore.rules` | Firestore ปฏิเสธเอง | **ข้ามไม่ได้** แม้เปิด F12 สั่งตรง ๆ |
| ซ่อนปุ่มในหน้าเว็บ | ไม่โชว์ปุ่มที่กดแล้ว error | ข้ามได้ แต่ชั้นล่างกันอยู่ |

ทดสอบสวมรอยด้วยการยิงคำสั่งตรงเข้า Firestore ข้ามหน้าเว็บแล้ว ทั้งบทบาท `employee` และ `hr`

---

## ตารางสิทธิ์

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา**<br>`employee` | • ยื่นใบขอลาใหม่<br>• ดูใบลาของตัวเอง<br>• เขียนความเห็นเพิ่มในใบของตัวเอง<br>• ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | • **อนุมัติหรือไม่อนุมัติใบของตัวเอง**<br>• เปลี่ยนสถานะใบลาของใครก็ตาม<br>• **ดูใบลาของคนอื่น**<br>• เขียนความเห็นในใบของคนอื่น<br>• แก้ประเภทการลา |
| ✅ **ผู้อนุมัติ**<br>`manager` | • ดูใบลาทุกใบ<br>• **เปลี่ยนสถานะใบลา** (อนุมัติ / ไม่อนุมัติ)<br>• เขียนความเห็นในทุกใบ | • ลบใบลาของคนอื่น<br>• แก้ประเภทการลา |
| 🧾 **ฝ่ายบุคคล**<br>`hr` | • ทุกอย่างที่ผู้อนุมัติทำได้<br>• **กำหนดผู้อนุมัติให้แต่ละใบ** ⚠️ *ยังไม่ได้ทำ*<br>• **เพิ่ม แก้ ลบ ประเภทการลา**<br>• เห็นแดชบอร์ดสรุปทั้งระบบ<br>&nbsp;&nbsp;*(หน้าโครงเท่านั้น · ต่อข้อมูลจริงใน Module 3)* | — ไม่มีข้อจำกัด |

---

> 🔑 **ข้อสำคัญที่สุดของตารางนี้: ผู้ขอลาอนุมัติใบของตัวเองไม่ได้**
> ต่อให้เป็นใบที่ตัวเองยื่น ก็กดอนุมัติเองไม่ได้ ต้องให้ `manager` หรือ `hr` เป็นคนกดเท่านั้น
> สิ่งที่ผู้ขอลาทำกับใบตัวเองได้มีแค่ **ยื่น · ดู · เขียนความเห็น · ลบตอนที่ยังรอพิจารณา**

> ❓ **ข้อที่ spec ยังไม่ได้ระบุ:** แล้ว `manager` อนุมัติใบลาของตัวเองได้ไหม
> [spec หัวข้อ 2](leaveeasy-spec.md) ไม่ได้เขียนห้ามไว้ ตารางนี้จึงไม่เขียนห้ามเช่นกัน
> **ถ้าจะห้าม ต้องถามผู้สอนก่อน** อย่าเพิ่งเขียนกฎเองในสัปดาห์ที่ 8

> ⚠️ **ข้อที่ยังไม่ได้ทำ: hr กำหนดผู้อนุมัติให้แต่ละใบ**
> `firestore.rules` อนุญาตให้แก้ใบลาได้แค่ช่อง `status` กับ `aiSuggestion` เท่านั้น
> **ยังไม่มีกฎให้แก้ `approverId` / `approverName`** และยังไม่มีหน้าจอให้ทำ
> เป็นการทำ**แคบกว่า**ที่ตารางกำหนด ไม่ใช่กว้างเกิน จึงไม่เป็นช่องโหว่ · ถ้าจะทำต้องเพิ่มทั้งกฎและหน้าจอ

## กฎที่ใช้กับทุกบทบาทเท่ากัน

กฎพวกนี้มาจาก [spec หัวข้อ 6](leaveeasy-spec.md) และ **บังคับในโค้ดแล้วตั้งแต่ตอนนี้** ไม่ต้องรอสัปดาห์ที่ 8

| กฎ | ทำไม |
|---|---|
| ใบใหม่เริ่มที่ `รอพิจารณา` เสมอ ผู้ใช้เลือกไม่ได้ | กันการยื่นใบที่อนุมัติมาแล้ว |
| ใบที่ `อนุมัติ` หรือ `ไม่อนุมัติ` แล้ว **เปลี่ยนต่อไม่ได้** ห้ามย้อนกลับ | ผลการพิจารณาต้องเป็นที่สิ้นสุด |
| จะกด `ไม่อนุมัติ` ต้องมีความเห็นอย่างน้อย 1 รายการก่อน | ผู้ขอลาต้องรู้เหตุผล |
| ลบใบลาได้เฉพาะใบที่ยังเป็น `รอพิจารณา` | ใบที่พิจารณาแล้วคือหลักฐาน |
| เปลี่ยนสถานะให้แก้เฉพาะช่อง `status` ห้ามเขียนทับช่องอื่น | กันข้อมูลอื่นในใบหายโดยไม่ตั้งใจ |
| สมัครสมาชิกได้ `role` เป็น `employee` เสมอ | ผู้ใช้ตั้งตัวเองเป็น `hr` ไม่ได้ |

**การเปลี่ยนบทบาทของใคร ต้องเข้าไปแก้ช่อง `role` ใน Firebase Console เท่านั้น** ไม่มีหน้าจอในระบบให้ทำ

---

## 🤖 ของที่เพิ่มเองนอก spec (ผู้ช่วย AI)

ไม่ได้อยู่ใน [spec หัวข้อ 5](leaveeasy-spec.md) — เพิ่มตามคำสั่งผู้ใช้ในสัปดาห์ที่ 8

| สิ่งที่เพิ่ม | ใครเขียนได้ | ใครอ่านได้ |
|---|---|---|
| ช่อง `aiSuggestion` ในใบลา | `manager` · `hr` | เหมือนใบลา (เจ้าของหรือผู้อนุมัติ) |
| โฟลเดอร์ย่อย `aiLog` | `manager` · `hr` | เหมือนใบลา |

**กฎเหล็ก: AI เปลี่ยนสถานะใบลาเองไม่ได้**
กฎที่ให้เขียน `aiSuggestion` **ไม่มี `status` อยู่ในก้อนนั้น** ต่อให้โค้ดพลาดหรือมีคนยิงคำสั่งตรง ๆ ก็เปลี่ยนสถานะไม่ผ่าน
สถานะจริงเปลี่ยนได้ทางเดียวคือคนกดปุ่มอนุมัติ/ไม่อนุมัติ ซึ่งผ่านกฎอีกก้อนที่เข้มกว่า

**`aiLog` แก้ไม่ได้ตลอดกาล** เพราะเป็นร่องรอยไว้ตรวจย้อนหลังว่าส่งอะไรไปให้ AI และได้อะไรกลับมา
ลบได้เฉพาะตอนที่กำลังลบใบลาทั้งใบเท่านั้น

---

## ค่าที่เก็บในฐานข้อมูล

ช่อง `role` ในโฟลเดอร์ `users` เก็บเป็น**รหัสภาษาอังกฤษ**เสมอ

```
employee · manager · hr
```

ส่วนคำไทย (ผู้ขอลา · ผู้อนุมัติ · ฝ่ายบุคคล) ใช้แสดงบนหน้าจอเท่านั้น
**ห้ามเก็บคำไทยลงช่อง `role`** เพราะสัปดาห์ที่ 8 จะเอาค่านี้ไปเขียน Security Rules โดยตรง

---

## สิ่งที่ยังไม่มีในระบบนี้

ตาม [spec หัวข้อ 9](leaveeasy-spec.md) — **ห้ามสร้างขึ้นมาเองแม้จะเห็นว่าควรมี**

- ไม่มีบทบาทที่ 4 ขึ้นไป — มี 3 บทบาทตามตารางนี้เท่านั้น
- ไม่มีการอนุมัติหลายขั้นหรือสายบังคับบัญชาหลายชั้น
- ไม่มีการเก็บประวัติว่าใครแก้อะไรเมื่อไร
