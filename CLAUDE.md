# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ภาพรวม

เว็บระบบขอลาออนไลน์ (LeaveEasy) เขียนด้วย **HTML · CSS · JavaScript ธรรมดา ไม่มี framework ไม่มีขั้นตอน build**
เป็นใบงานของหลักสูตร ADT-RAISE Batch 2 Module 2 — ไม่ใช่ระบบใช้งานจริง

## คำสั่ง

```bash
npm install      # ครั้งเดียวตอนเริ่ม (ลง serve)
npm run dev      # เปิด http://localhost:3000
```

**ไม่มี test และไม่มี linter ในโปรเจกต์นี้** — spec ห้ามเขียนทดสอบอัตโนมัติจนกว่าจะถึงสัปดาห์ที่ 9
วิธีตรวจงานคือเปิดเบราว์เซอร์ดูจริง

⚠️ **ต้องเปิดผ่าน `npm run dev` เท่านั้น** — หน้าที่ใช้ Firestore เป็น ES module เปิดด้วย `file://` (ดับเบิลคลิก) ไม่ได้

## เอกสารที่ต้องอ่านก่อนแก้อะไร

**`leaveeasy-spec.md` คือแหล่งความจริง** — กำหนดชื่อโฟลเดอร์ ชื่อช่องข้อมูล ค่าสถานะ และข้อมูลตัวอย่าง
ก่อนตั้งชื่อช่องใหม่หรือเพิ่มโครงสร้างข้อมูล ให้เปิดหัวข้อ 5 และ 7 ก่อนเสมอ

### 🛑 ขอบเขตรายสัปดาห์ — ห้ามทำงานล่วงหน้า

spec หัวข้อ 8 แบ่งงานเป็นสัปดาห์ และหัวข้อ 9 เขียนเตือน AI ไว้ตรง ๆ ว่า **ห้ามสร้างสิ่งที่อยู่นอกขอบเขตขึ้นมาเอง แม้จะเห็นว่าระบบควรมี** ถ้าคิดว่าจำเป็น ให้เสนอแล้วรอคำสั่ง

| สัปดาห์ | ขอบเขต |
|:-:|---|
| 6 | อ่านจาก Firestore อย่างเดียว (R ตัวเดียว) |
| 7 | CRUD ครบ · ล็อกอิน · Firebase Hosting |
| 8 | Security Rules · ปุ่มผู้ช่วย AI |
| 9 | ทดสอบอัตโนมัติ |

**สถานะปัจจุบัน: กำลังทำสัปดาห์ที่ 7 — CRUD ครบ 4 ตัวแล้ว**

| งานสัปดาห์ที่ 7 | สถานะ |
|---|:-:|
| C สร้างใบลาใหม่ลง Firestore | ✅ |
| R อ่านรายการและรายละเอียด | ✅ |
| U แก้ (อนุมัติ/ไม่อนุมัติ · ส่งความเห็น) | ✅ |
| D ลบ (เฉพาะใบที่ยัง `รอพิจารณา`) | ✅ |
| ล็อกอิน Firebase Authentication | ⬜ |
| กฎขั้นต่ำ ต้องล็อกอินก่อนถึงอ่าน/เขียนได้ | ⬜ |
| Firebase Hosting | ⬜ |
| `leave-types.html` ต่อ Firestore | ⬜ |

⚠️ **ตอนเปลี่ยนสถานะ ต้องใช้ `updateDoc` ส่งไปเฉพาะช่อง `status`** — [spec หัวข้อ 6](leaveeasy-spec.md) สั่งห้ามเขียนทับช่องอื่น
แปลว่ากดอนุมัติแล้ว **ห้ามเติม `approverId`/`approverName`** แม้จะดูสมเหตุสมผลก็ตาม · ถ้าใช้ `setDoc` จะลบอีก 11 ช่องทิ้งทั้งไฟล์

⚠️ **ลบใบลาต้องลบความเห็นในโฟลเดอร์ย่อย `approvals` ให้หมดก่อน** — Firestore ไม่ลบโฟลเดอร์ย่อยตามให้อัตโนมัติ
ถ้าลบแต่ไฟล์แม่ ความเห็นจะค้างเป็นขยะที่มองไม่เห็นทั้งจากหน้าเว็บและจาก Console

⚠️ **ยังไม่มีการกันสิทธิ์จริง** — ปุ่มที่ `disabled` กันได้แค่ผู้ใช้ทั่วไป เปิด F12 สั่ง Firestore ตรง ๆ ยังทำได้
spec ให้ทำ Security Rules ในสัปดาห์ที่ 8 · อย่าพยายามกันด้วยโค้ดฝั่งหน้าเว็บอย่างเดียว

ข้อห้ามถาวรจาก spec หัวข้อ 9: ไม่ใช้ framework · ไม่เขียนเซิร์ฟเวอร์เอง · ไม่ใช้ SQL · ไม่ทำ pagination · **ห้ามใส่ข้อมูลจริงของบุคคล**

## สถาปัตยกรรม

### สคริปต์สองรุ่นอยู่ร่วมกัน — จุดที่ต้องเข้าใจก่อนแก้ HTML

ทุกหน้าโหลด `js/util.js` และ `js/nav.js` เป็น **classic script + `defer`** ซึ่งประกาศฟังก์ชันไว้บน `window`
(`esc()` · `ป้ายสถานะ()` · `เวลาตอนนี้()` · `ค่าจากURL()` · `ข้อความผิดพลาดฐานข้อมูล()`)

ส่วนสคริปต์ประจำหน้ามี 2 แบบปนกัน

| หน้า | สคริปต์ | แหล่งข้อมูล |
|---|---|---|
| `leave-requests.html` | `type="module"` | **Firestore** (อ่านอย่างเดียว) |
| `leave-request-detail.html` | `type="module"` | **Firestore** (อ่าน · แก้ `status` · เพิ่มความเห็น · ลบใบลา) |
| `new-leave-request.html` | `type="module"` | **Firestore** (อ่าน `leaveTypes` · เขียนใบลาใหม่) |
| `leave-types.html` | classic `defer` + `js/data.js` | ข้อมูลปลอมในไฟล์ — **เหลือหน้าเดียว** |

**ที่ผสมกันได้เพราะ** classic `defer` ทำงานก่อน module เสมอตามลำดับที่เขียนใน HTML — module จึงเรียกฟังก์ชันจาก `util.js` ได้
เวลาแปลงหน้าที่เหลือไปใช้ Firestore: ลบบรรทัด `data.js` ออก แล้วเปลี่ยน `defer` เป็น `type="module"` และ **คง `util.js` กับ `nav.js` ไว้ที่เดิม**

`js/firebase.js` เป็น module ที่ `export const db` — หน้าที่ต้องใช้ Firestore เขียน `import { db } from "./firebase.js"`

### โครงสร้างข้อมูลบน Firestore

```
users/{u001}          { name, email, role }
leaveTypes/{lt001}    { name }
leaveRequests/{lr001} { title, reason, status,
                        requesterId+requesterName, approverId+approverName,
                        leaveTypeId+leaveTypeName, startDate, endDate, createdAt }
  └ approvals/{ap001} { authorId, authorName, message, createdAt }
```

กติกา 3 ข้อที่พังเงียบ ๆ ถ้าทำผิด

1. **รหัสคือชื่อไฟล์ (document ID) ไม่ใช่ช่องข้อมูล** — ห้ามเขียนช่อง `id` ลงในเอกสาร ตอนอ่านให้ประกอบเอง `Object.assign({ id: doc.id }, doc.data())` ถ้าลืม ปุ่มกดแถวจะเปิดใบลาไม่ได้
2. **จดชื่อซ้ำคู่กับรหัสเสมอ** — Firestore ไม่มี JOIN ยอมรับว่าถ้ามีคนเปลี่ยนชื่อใน `users` ใบลาเก่าจะยังขึ้นชื่อเดิม (spec สั่งให้ยอมรับ ไม่ต้องเขียนระบบไล่อัปเดต)
3. **ตัวพิมพ์เล็ก-ใหญ่ของชื่อช่องต้องตรงทั้งระบบ** — `status` กับ `Status` เป็นคนละช่อง

### สถานะผูกกับ CSS โดยตรง

`status` มี 3 ค่าเท่านั้น: `รอพิจารณา` · `อนุมัติ` · `ไม่อนุมัติ` (เปลี่ยนได้ทางเดียว ห้ามย้อนกลับ)

`ป้ายสถานะ()` ใน `js/util.js` สร้าง class ชื่อ `badge-` + ค่าสถานะ ซึ่งไปตรงกับ `.badge-รอพิจารณา` ฯลฯ ใน `css/style.css`
**แก้ข้อความสถานะเมื่อไหร่ ป้ายสีหายทันทีโดยไม่มี error** ต้องแก้ CSS ตามด้วย

### `serve.json` — ห้ามลบ

ตั้ง `cleanUrls: false` ไว้ เพราะค่าเริ่มต้นของ `serve` จะตัด `.html` ออกแล้ว **ทิ้ง query string ไปด้วย**
ทำให้ `leave-request-detail.html?id=lr001` กลายเป็น `/leave-request-detail` และหน้ารายละเอียดหาใบลาไม่เจอ

## ธรรมเนียมการเขียนโค้ด

- **ตัวแปร ฟังก์ชัน และคอมเมนต์เป็นภาษาไทย** ส่วนชื่อช่องข้อมูลและ API ของ Firebase เป็นอังกฤษ — เขียนโค้ดใหม่ให้เข้ากับแบบนี้
- ใช้ `var` + `function` แบบเดิม ไม่ใช้ arrow function หรือ class (ยกเว้น `import`/`async` ในไฟล์ module)
- ทุกข้อความจากผู้ใช้ต้องผ่าน `esc()` ก่อนเอาไปต่อเป็น HTML
- ข้อความ error ต้องบอกสาเหตุเป็นภาษาคน ไม่ปล่อยจอค้างที่ "กำลังโหลดข้อมูล…"
- ข้อความ commit เป็นภาษาไทย
